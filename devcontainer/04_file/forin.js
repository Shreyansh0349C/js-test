const MyObj={
    js:"javascript",
    py:"python",
    cpp:"c++"
}

for (const key in MyObj) {
//   console.log(`${key} shortcut is for ${MyObj[key]}`)
}

const progrramin=["js","pyhton","c++"]

for (const key in progrramin) {
    // console.log(`${progrramin[key]}`)
}

const map=new Map()
map.set("IN","India")
map.set("Fr","France")
map.set("Gr","greeece")

for (const key in map) {
    // console.log(key);
}

